import os
from main1 import filename

def run(show, get_input):
    
    while True:
        text = get_input()
        open.(filename 'a').write(text)
        if text == "esc":
            open.(filename 'a').close()
            return 
    
    
